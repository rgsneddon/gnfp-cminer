Place the gnfp-cminer binary next to this note, or run `make` from the repo root.
The Darwin pack copies the compiled binary to the tree root as gnfp-cminer.
Edit example.sh (gnfp1 address + .worker + --threads), then ./example.sh.
